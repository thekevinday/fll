# fss-000d

settings:
  name "Environment default"
  engine bash

script:
  start {
    echo
    echo "==================================="
    echo "Environment using default settings."
    echo "==================================="

    env
  }

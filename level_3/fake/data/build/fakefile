# fss-0005 iki-0002

settings:
  load_build yes
  fail exit

  compiler gcc
  linker ar

  environment

  parameter verbose +v
  parameter verbose_fll +V

main:

  #build

  print beginning
  if is block xxx character directory fifo link fifo regular socket invalid for a b c
    print success detected.
  else
    print no success detected.
